import type { SettingsLocaleTable } from "../types";

/** Brazilian Portuguese overrides for the context tab. Filled by the pt-BR localization work. */
export const context: SettingsLocaleTable = {};
