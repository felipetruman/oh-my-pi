import type { SettingsLocaleTable } from "../types";

/** Brazilian Portuguese overrides for the interaction tab. Filled by the pt-BR localization work. */
export const interaction: SettingsLocaleTable = {};
