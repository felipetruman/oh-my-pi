import type { SettingsLocaleTable } from "../types";

/** Brazilian Portuguese overrides for the providers tab. Filled by the pt-BR localization work. */
export const providers: SettingsLocaleTable = {};
